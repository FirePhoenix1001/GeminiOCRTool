```
我想添加一些功能
1.最高優先：先前我在使用時發生了一個問題當我按下"開始進行轉換時"如果這個時候我去操作其他東西，例如"AI優化規則"正在辨識的AI會強制停止，我懷疑如果我再生成詳解時，去操作其他功能應該也會被中斷，或是如果我在"AI優化規則"，去生成詳解，或是辨識OCR，因該也會被強制中斷，所以我想要新增的功能是不會被強制中斷
2.強制停止：我希望在"辨識OCR"還有"生成詳解"時，多一個中斷，目的是當如果文本太長想中斷工作就可以不用直接關掉軟體  
```
```
我想要新增一些功能
1.任務接續：先前我在"OCR辨識"時，我意外中斷軟體，但是我有為此設一個保險，就是每次生成時都把內容放進文字文件，理論來說這樣就不怕中斷只要下次接著繼續辨識就好，但是我發現軟體竟然重新生成一個文字文件並解把舊的刪掉那這個保賢不就沒用了嗎，所以我想要的功能是避免這個情況發生並且接續文字文件的內容，最後把內容匯進word時要以文字文件的內容為主，避免使用者如果修改時，修改文字文件但是word結果沒有變，"生成詳解"也要
2.自定義命名：為了讓使用者可以自由的編輯最後word結果的檔案名稱，我想讓使用者自定義命名，功能我想要添加在，，就是原本那個地方是顯示檔案名稱的，但是當滑鼠移過去就可以自定義word結果的名稱，以.doc為後綴，如果使用者沒有修改預設顯示就是原本.pdf檔案的名稱，只是以.doc為後綴顯示，但是如果檔案是圖片類型的就不要有這個功能，"生成詳解"也要
```
```
還有一點小細節要修改
1.滑鼠懸停還有一些問題，理論上我滑鼠移過去如果沒有去點擊應該要顯示.pdf後綴的檔案名，但是他仍然顯示.doc後綴的檔案名
2.相同的檔案不能重複選取
3.暫存的.txt檔名應該以自定義的檔名為主，而不是原本.pdf的檔名
4.所有可以讓使用者輸入的文字框在點擊空白處應該要取消選取，而不是停留在輸入狀態
5.某些可以讓使用者輸入的文字框輸入中文時有一些問題，例如："("和")"無法輸入，換變成選取
6.當成功完成辨識時應該要刪除.txt除非意外暫停或停止
```
```
還有一點小細節要修改
1.懸浮的功能依然沒有解決，你自己看終端機
2.我想讓相同的檔案可以重複選取
3."及時編輯與輸出"的文字框只要輸出圖片的辨識結果，.pdf的不用
```
```
還有一點小細節要修改
1.懸浮的功能依然沒有解決，你自己看終端機，目前的問題是滑鼠移過去確實會顯示.doc後綴，但是滑鼠移開後沒有恢復原本的.pdf後綴
2.能夠修改的只有檔案名稱後綴是不可以讓使用者修改的，所以滑鼠一過去應該顯示可以編輯的檔案明和不可編輯的後綴
```
```
我想要新增一些功能
1.檔案列表優化：當使用者按下"開始進行轉換"時，檔案列表就顯示.doc後綴的名稱(不可編輯的)，當滑鼠移動過去，就顯示原本的檔案，就是.pdf後綴的檔案，然後"頁數"和"移除"可以不用顯示，當滑鼠移移開時，就恢復原本的.doc後綴的名稱
```
```
還有一點小細節要修改
1.我之前有請你做過懸浮的功能就是滑鼠移過去會顯示.doc後綴個那個功能，現在出了一個問題，滑鼠移動過去.doc後綴和.pdf後綴會瘋狂來回顯示
```

### 未來功能
1. GPT分頁